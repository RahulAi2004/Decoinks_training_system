services:
  app:
    build: .
    container_name: decoinks-training-system
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
      - ./content/company-products:/app/content/company-products
